{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": false,
    "emitDeclarationOnly": false,
    "noEmit": false
  },
  // Test helpers and the recorded fixtures are not part of the shipped surface.
  "exclude": [
    "node_modules",
    "dist",
    "src/test-utils/**",
    "**/*.test.ts",
    "**/*.test.tsx"
  ]
}
