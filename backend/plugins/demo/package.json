{
  "name": "@tetherto/mdk-plugin-demo",
  "version": "0.9.0",
  "description": "MDK gateway plugin: aggregates live telemetry and history from the demo-worker sample across registered devices.",
  "author": "Tether",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "keywords": [
    "mdk",
    "mdk-plugin"
  ],
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "NODE_ENV=test brittle tests/*.test.js",
    "test:coverage": "NODE_ENV=test brittle --coverage tests/*.test.js"
  },
  "dependencies": {
    "@tetherto/mdk-client": "file:../../core/client"
  },
  "devDependencies": {
    "@tetherto/mdk-gateway": "file:../../core/gateway",
    "brittle": "3.18.0",
    "standard": "17.1.0"
  }
}
