{
  "note": "GENERATED by docs/scripts/generate-plugin-reference.js from backend/core/plugins/*/mdk-plugin.json and backend/plugins/*/mdk-plugin.json. Do not edit by hand. This is the machine-readable twin of docs/reference/supported-plugins.md. Read that instead if you're a person, not a script.",
  "source": "backend/core/plugins/*/mdk-plugin.json and backend/plugins/*/mdk-plugin.json",
  "plugins": [
    {
      "bucket": "Bundled site plugins",
      "name": "@tetherto/mdk-plugin-site-hashrate",
      "dir": "site-hashrate",
      "root": "backend/core/plugins",
      "routes": [
        {
          "method": "GET",
          "path": "/api/site/hashrate-history",
          "description": "Fans out telemetry.pull to every registered worker and returns site-level hashrate history aggregated by timestamp. Defaults to last 7 days when start/end are omitted."
        }
      ]
    },
    {
      "bucket": "Bundled site plugins",
      "name": "@tetherto/mdk-plugin-site-monitor",
      "dir": "site-monitor",
      "root": "backend/core/plugins",
      "routes": [
        {
          "method": "GET",
          "path": "/auth/site",
          "description": "Returns the site name from the gateway config (common.json `site`)."
        },
        {
          "method": "GET",
          "path": "/auth/featureConfig",
          "description": "Returns the featureConfig object from the gateway config (common.json `featureConfig`)."
        },
        {
          "method": "GET",
          "path": "/site-monitor/hashrate",
          "description": "Pulls metrics telemetry from every READY worker's devices via the MDK protocol and returns per-device hashrate/power plus site totals."
        }
      ]
    },
    {
      "bucket": "Bundled site plugins",
      "name": "@tetherto/mdk-plugin-telemetry",
      "dir": "telemetry",
      "root": "backend/core/plugins",
      "routes": [
        {
          "method": "GET",
          "path": "/auth/metrics/hashrate",
          "description": "Returns daily hashrate history and summary for the site. Optionally groups by miner type or container."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/consumption",
          "description": "Returns daily power consumption (W and MWh) history and summary for the site."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/efficiency",
          "description": "Returns daily mining efficiency (W/TH) history and summary for the site."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/miner-status",
          "description": "Returns daily online/offline/sleep/maintenance miner counts and averages."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/power-mode",
          "description": "Returns miner count by power mode category (low/normal/high/sleep/offline) over time."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/power-mode/timeline",
          "description": "Returns per-miner power mode segments over a time range, optionally filtered by container."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/temperature",
          "description": "Returns max and average temperature per container over time, with site-level aggregates."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/containers/{id}",
          "description": "Returns latest telemetry snapshot and miner list for a specific container."
        },
        {
          "method": "GET",
          "path": "/auth/metrics/containers/{id}/history",
          "description": "Returns historical telemetry log for a specific container."
        }
      ]
    },
    {
      "bucket": "Inert",
      "name": "@tetherto/mdk-plugin-auth",
      "dir": "auth",
      "root": "backend/core/plugins",
      "routes": [
        {
          "method": "GET",
          "path": "/auth/userinfo",
          "description": "Returns the authenticated user's profile from the validated JWT."
        },
        {
          "method": "POST",
          "path": "/auth/token",
          "description": "Issues a new JWT from an existing valid token, optionally scoping TTL and roles."
        },
        {
          "method": "GET",
          "path": "/auth/permissions",
          "description": "Returns the permission set encoded in the current token."
        },
        {
          "method": "GET",
          "path": "/auth/ext-data",
          "description": "Proxies an external data request to the Kernel network by type and optional query filter."
        }
      ]
    },
    {
      "bucket": "Optional plugins",
      "name": "@tetherto/mdk-plugin-agent",
      "dir": "agent",
      "root": "backend/plugins",
      "routes": [
        {
          "method": "POST",
          "path": "/agent/sessions",
          "description": "Opens an agent session bound to the caller's identity."
        },
        {
          "method": "POST",
          "path": "/agent/sessions/:id/messages",
          "description": "Streams an agent turn over SSE; write tools pause for an operator's approval."
        },
        {
          "method": "POST",
          "path": "/agent/sessions/:id/approvals/:approvalId",
          "description": "Approves or rejects a paused write, resuming or ending the turn."
        },
        {
          "method": "DELETE",
          "path": "/agent/sessions/:id",
          "description": "Deletes a session owned by the caller."
        }
      ]
    },
    {
      "bucket": "Optional plugins",
      "name": "@tetherto/mdk-plugin-demo",
      "dir": "demo",
      "root": "backend/plugins",
      "routes": [
        {
          "method": "GET",
          "path": "/api/demo/summary",
          "description": "Fans out metrics telemetry to every registered demo-worker device (fingerprint: hashrate_rt + history) and returns fleet totals plus a per-device breakdown. Aggregation lives here — workers only ever answer for one device."
        },
        {
          "method": "GET",
          "path": "/api/demo/history",
          "description": "Pulls the demo-worker `history` telemetry channel (SQLite recent samples: ts, hashrate_ths, power_w, board_temp_c) for every matched device, or a single device when deviceId is set."
        }
      ]
    }
  ]
}
