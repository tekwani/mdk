# MDK monorepo (mdk): backend/core/*, backend/workers/**, ui (all npm).
# Aligned with MiningOS CI Checks: workspace-aware install/cache, node cache actions, parallel jobs, PR audits.
# npm workspace members (see root package.json `workspaces`) install once at the
# repo root and share one hoisted node_modules cache; standalone packages (ui,
# backend/core/plugins, the example ui apps) install in place. The membership is
# resolved by .github/scripts/workspace-context.sh, so a package moving in or out
# of `workspaces` needs no workflow change.
name: CI Checks

on:
  push:
    branches: [main, develop, staging, "release/**"]
  pull_request:
    branches: [main, develop, staging, "release/**"]
    types: [opened, reopened, synchronize]
  workflow_dispatch:
    inputs:
      target:
        description: "Which pipeline to run"
        required: true
        default: auto
        type: choice
        options:
          - auto
          - ui
          - core
          - workers
          - examples
          - all

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "lts/*"
  UI_DIR: ui


jobs:
  detect-target:
    name: Detect changed areas
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      run_ui: ${{ steps.flags.outputs.run_ui }}
      run_core: ${{ steps.flags.outputs.run_core }}
      run_workers: ${{ steps.flags.outputs.run_workers }}
      run_examples: ${{ steps.flags.outputs.run_examples }}
      reason: ${{ steps.flags.outputs.reason }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Detect changed paths
        id: changed
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          ui=false; core=false; workers=false; examples=false; docs_only=false; ci_infra=false
          if [ "$EVENT_NAME" != "workflow_dispatch" ]; then
            if [ -z "$BASE_SHA" ] || [ "$BASE_SHA" = "0000000000000000000000000000000000000000" ]; then
              git fetch origin main --depth=1 2>/dev/null || true
              BASE_SHA=$(git merge-base HEAD origin/main 2>/dev/null || echo "")
            fi
            [ -n "$BASE_SHA" ] && CHANGED=$(git diff --name-only "${BASE_SHA}...${HEAD_SHA}") || CHANGED=""
            echo "$CHANGED" | grep -q "^ui/" && ui=true || true
            echo "$CHANGED" | grep -q "^backend/core/"      && core=true || true
            echo "$CHANGED" | grep -q "^backend/workers/"   && workers=true || true
            echo "$CHANGED" | grep -q "^examples/"          && examples=true || true
            # CI infra (workflows/composite actions/scripts) affects every domain,
            # so a change here must run all suites — even if only one domain's
            # files also changed (otherwise the CI change rides along untested).
            echo "$CHANGED" | grep -qE '^\.github/(workflows|actions|scripts)/' && ci_infra=true || true
            # docs/meta-only: every changed file is docs or repo-meta with no build/runtime
            # impact. Markdown link correctness is covered separately by link-check.yml, so
            # these changes need none of the domain build/test suites. Anything NOT on this
            # allowlist (root package.json, ci.yml, .gitignore, composite actions, source...)
            # falls through to the conservative run-all below.
            if [ -n "$CHANGED" ]; then
              NON_DOCS=$(printf '%s\n' "$CHANGED" | grep -vE '(^docs/|\.md$|^LICENSE$|^linkinator\.config\.json$)' || true)
              [ -z "$NON_DOCS" ] && docs_only=true
            fi
          fi
          echo "ui=$ui"             >> "$GITHUB_OUTPUT"
          echo "core=$core"         >> "$GITHUB_OUTPUT"
          echo "workers=$workers"   >> "$GITHUB_OUTPUT"
          echo "examples=$examples" >> "$GITHUB_OUTPUT"
          echo "docs_only=$docs_only" >> "$GITHUB_OUTPUT"
          echo "ci_infra=$ci_infra" >> "$GITHUB_OUTPUT"

      - name: Resolve which pipelines should run
        id: flags
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_TARGET: ${{ github.event_name == 'workflow_dispatch' && inputs.target || 'auto' }}
          UI_CHANGED: ${{ steps.changed.outputs.ui }}
          CORE_CHANGED: ${{ steps.changed.outputs.core }}
          WORKERS_CHANGED: ${{ steps.changed.outputs.workers }}
          EXAMPLES_CHANGED: ${{ steps.changed.outputs.examples }}
          DOCS_ONLY: ${{ steps.changed.outputs.docs_only }}
          CI_INFRA: ${{ steps.changed.outputs.ci_infra }}
        run: |
          run_ui=false
          run_core=false
          run_workers=false
          run_examples=false
          reason=""

          has_ui_lock=false
          [ -f "${{ env.UI_DIR }}/package-lock.json" ] && has_ui_lock=true

          if [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$INPUT_TARGET" != "auto" ]; then
            case "$INPUT_TARGET" in
              ui)       run_ui=true;       run_core=false; run_workers=false; run_examples=false; reason="manual=ui" ;;
              core)     run_ui=false;      run_core=true;  run_workers=false; run_examples=false; reason="manual=core" ;;
              workers)  run_ui=false;      run_core=false; run_workers=true;  run_examples=false; reason="manual=workers" ;;
              examples) run_ui=false;      run_core=false; run_workers=false; run_examples=true;  reason="manual=examples" ;;
              all)      run_ui=true;       run_core=true;  run_workers=true;  run_examples=true;  reason="manual=all" ;;
              *)        run_ui=true;       run_core=true;  run_workers=true;  run_examples=true;  reason="manual=unknown-fallback-all" ;;
            esac
          elif [ "$CI_INFRA" = "true" ]; then
            # CI workflow/actions changed → validate every domain.
            run_ui=$([ "$has_ui_lock" = "true" ] && echo true || echo false)
            run_core=true
            run_workers=true
            run_examples=true
            reason="auto=ci-infra-change->run-all"
          else
            if [ "$UI_CHANGED" = "true" ] && [ "$has_ui_lock" = "true" ]; then run_ui=true; fi
            if [ "$CORE_CHANGED" = "true" ]; then run_core=true; fi
            if [ "$WORKERS_CHANGED" = "true" ]; then run_workers=true; fi
            if [ "$EXAMPLES_CHANGED" = "true" ]; then run_examples=true; fi
            if [ "$run_ui" = "false" ] && [ "$run_core" = "false" ] && [ "$run_workers" = "false" ] && [ "$run_examples" = "false" ]; then
              if [ "$DOCS_ONLY" = "true" ]; then
                # docs/meta-only change: nothing to build or test here (link-check.yml
                # handles markdown links in its own workflow). Skip all domain suites.
                reason="auto=docs/meta-only->skip-domains"
              else
                # No domain matched but the change touches shared/root files that can
                # affect any domain (root package.json, ci.yml, .gitignore, ...): run all.
                run_ui=$([ "$has_ui_lock" = "true" ] && echo true || echo false)
                run_core=true
                run_workers=true
                run_examples=true
                reason="auto=no-targeted-change->run-all"
              fi
            else
              reason="auto=changed-paths"
            fi
          fi

          echo "run_ui=$run_ui" >> "$GITHUB_OUTPUT"
          echo "run_core=$run_core" >> "$GITHUB_OUTPUT"
          echo "run_workers=$run_workers" >> "$GITHUB_OUTPUT"
          echo "run_examples=$run_examples" >> "$GITHUB_OUTPUT"
          echo "reason=$reason" >> "$GITHUB_OUTPUT"
          echo "Selected: ui=$run_ui core=$run_core workers=$run_workers examples=$run_examples ($reason)"

  list-workers:
    name: List worker packages
    runs-on: ubuntu-latest
    needs: [detect-target]
    if: needs.detect-target.outputs.run_workers == 'true'
    outputs:
      dirs: ${{ steps.list.outputs.dirs }}
      buildable_dirs: ${{ steps.list.outputs.buildable_dirs }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Discover worker package directories
        id: list
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t dirs < <(
            find backend/workers -mindepth 2 -name package.json -not -path '*/node_modules/*' \
              | xargs -I{} dirname {} | sort -u
          )
          if [ "${#dirs[@]}" -eq 0 ]; then
            echo "::error::No package.json under backend/workers/"
            exit 1
          fi
          json=$(printf '%s\n' "${dirs[@]}" | jq -R . | jq -s -c .)
          echo "dirs=${json}" >> "$GITHUB_OUTPUT"
          echo "Worker packages: ${json}"
          buildable=()
          for dir in "${dirs[@]}"; do
            node -e "const s=require('./${dir}/package.json').scripts||{}; process.exit(s.build?0:1)" 2>/dev/null \
              && buildable+=("$dir") || true
          done
          if [ "${#buildable[@]}" -gt 0 ]; then
            buildable_json=$(printf '%s\n' "${buildable[@]}" | jq -R . | jq -s -c .)
          else
            buildable_json='[]'
          fi
          echo "buildable_dirs=${buildable_json}" >> "$GITHUB_OUTPUT"
          echo "Buildable worker packages: ${buildable_json}"

  list-core:
    name: List core packages
    runs-on: ubuntu-latest
    needs: [detect-target]
    if: needs.detect-target.outputs.run_core == 'true'
    outputs:
      dirs: ${{ steps.list.outputs.dirs }}
      test_dirs: ${{ steps.list.outputs.test_dirs }}
      buildable_dirs: ${{ steps.list.outputs.buildable_dirs }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Discover core package directories
        id: list
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t dirs < <(
            find backend/core -mindepth 2 -name package.json -not -path '*/node_modules/*' \
              | xargs -I{} dirname {} | sort -u
          )
          if [ "${#dirs[@]}" -eq 0 ]; then
            echo "::error::No package.json under backend/core/"
            exit 1
          fi
          json=$(printf '%s\n' "${dirs[@]}" | jq -R . | jq -s -c .)
          echo "dirs=${json}" >> "$GITHUB_OUTPUT"
          echo "Core packages: ${json}"
          # mdk is tested via the sharded test-mdk/coverage-mdk jobs (its integration
          # suite is too slow for a single runner), so exclude it from the generic
          # per-package test matrix. It stays in `dirs` for lint/typecheck/build.
          test_json=$(printf '%s\n' "${dirs[@]}" | grep -v '^backend/core/mdk$' | jq -R . | jq -s -c .)
          echo "test_dirs=${test_json}" >> "$GITHUB_OUTPUT"
          echo "Core test packages (mdk sharded separately): ${test_json}"
          buildable=()
          for dir in "${dirs[@]}"; do
            node -e "const s=require('./${dir}/package.json').scripts||{}; process.exit(s.build?0:1)" 2>/dev/null \
              && buildable+=("$dir") || true
          done
          if [ "${#buildable[@]}" -gt 0 ]; then
            buildable_json=$(printf '%s\n' "${buildable[@]}" | jq -R . | jq -s -c .)
          else
            buildable_json='[]'
          fi
          echo "buildable_dirs=${buildable_json}" >> "$GITHUB_OUTPUT"
          echo "Buildable core packages: ${buildable_json}"

  list-examples:
    name: List example packages
    runs-on: ubuntu-latest
    needs: [detect-target]
    if: needs.detect-target.outputs.run_examples == 'true'
    outputs:
      dirs: ${{ steps.list.outputs.dirs }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Discover example package directories
        id: list
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t dirs < <(
            find examples -name package.json -not -path '*/node_modules/*' \
              | xargs -I{} dirname {} | sort -u
          )
          if [ "${#dirs[@]}" -eq 0 ]; then
            echo "::error::No package.json under examples/"
            exit 1
          fi
          json=$(printf '%s\n' "${dirs[@]}" | jq -R . | jq -s -c .)
          echo "dirs=${json}" >> "$GITHUB_OUTPUT"
          echo "Example packages: ${json}"

  setup-examples:
    name: 📦 Setup Example (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-examples]
    if: needs.detect-target.outputs.run_examples == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-examples.outputs.dirs) }}
    outputs:
      cache-key: ${{ steps.setup-deps.outputs.cache-key }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Install dependencies and cache node_modules
        id: setup-deps
        uses: ./.github/actions/node-setup-cache
        with:
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}

  test-examples:
    name: 🧪 Test Example (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [detect-target, list-examples, setup-examples]
    if: needs.detect-target.outputs.run_examples == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-examples.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-examples.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      # Examples relative-require the shared core/worker packages. Those are all
      # npm workspace members, so one install at the workspace root hoists their
      # deps and the requires resolve. Examples are run but not coverage-gated.
      - name: Ensure workspace dependencies (sibling resolution)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          # Member jobs already restored the root node_modules above (no-op here);
          # standalone example jobs install it alongside their own node_modules.
          if [ ! -d node_modules ] || [ -z "$(ls -A node_modules 2>/dev/null)" ]; then
            npm ci --ignore-scripts --no-audit --no-fund
            for pkg in sqlite3 bcrypt; do
              if npm ls "$pkg" --depth=99 --parseable 2>/dev/null | grep -qE "(^|/)${pkg}$"; then
                npm rebuild "$pkg"
              fi
            done
          fi
      - run: npm test --if-present

  setup-ui:
    name: 📦 Setup UI (ui)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target]
    if: needs.detect-target.outputs.run_ui == 'true'
    outputs:
      cache-key: ${{ steps.setup-deps.outputs.cache-key }}
      lockfile-outcome: ${{ steps.lockfile.outcome }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Lockfile consistency check
        id: lockfile
        shell: bash
        working-directory: ${{ env.UI_DIR }}
        run: |
          set -euo pipefail
          if [ ! -f package-lock.json ]; then
            echo "No package-lock.json; skipping lockfile check"
            exit 0
          fi
          npm install --package-lock-only --ignore-scripts --no-audit --no-fund
          git -C "${GITHUB_WORKSPACE}" diff --exit-code "${{ env.UI_DIR }}/package-lock.json" || (
            echo "::error::package-lock.json is out of sync with package.json."
            echo "::error::Regenerate with: npm install --package-lock-only --ignore-scripts --no-audit --no-fund"
            exit 1
          )

      - name: Install dependencies and cache node_modules
        id: setup-deps
        uses: ./.github/actions/node-setup-cache
        with:
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}

  lint-ui:
    name: ✨ Lint UI
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [detect-target, setup-ui]
    if: needs.detect-target.outputs.run_ui == 'true'
    defaults:
      run:
        working-directory: ${{ env.UI_DIR }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-ui.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}
      - run: npm run lint --if-present

  typecheck-ui:
    name: 🔎 Typecheck UI
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, setup-ui]
    if: needs.detect-target.outputs.run_ui == 'true'
    defaults:
      run:
        working-directory: ${{ env.UI_DIR }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-ui.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}
      - run: npm run typecheck --if-present

  # 🧪 Test UI — sharded matrix.
  # react-devkit is the bottleneck (~13 min, 296 test files), so it is split
  # across 4 parallel runners via `vitest --shard`. Each shard writes a blob
  # report; the coverage-ui job merges them and enforces thresholds. The smaller
  # packages (ui-foundation, react-adapter, cli) run once on shard 1 via turbo.
  test-ui:
    name: 🧪 Test UI (shard ${{ matrix.shard }}/4)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, setup-ui]
    if: needs.detect-target.outputs.run_ui == 'true'
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    env:
      NODE_OPTIONS: --max-old-space-size=4096
    defaults:
      run:
        working-directory: ${{ env.UI_DIR }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-ui.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}

      - name: Cache Turbo
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ${{ env.UI_DIR }}/.turbo
          key: turbo-ui-test-${{ runner.os }}-${{ hashFiles('ui/package-lock.json') }}-${{ github.sha }}-shard-${{ matrix.shard }}
          restore-keys: |
            turbo-ui-${{ runner.os }}-${{ hashFiles('ui/package-lock.json') }}-
            turbo-ui-${{ runner.os }}-

      - name: Cache Vitest transforms
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          # All tested packages set `cacheDir: '.cache'` in vitest.config.js.
          path: |
            ui/packages/react-devkit/.cache
            ui/packages/ui-foundation/.cache
            ui/packages/react-adapter/.cache
            ui/packages/cli/.cache
          key: vitest-ui-${{ runner.os }}-${{ hashFiles('ui/packages/**/*.ts', 'ui/packages/**/*.tsx', 'ui/packages/**/vitest.config.js') }}-shard-${{ matrix.shard }}
          restore-keys: |
            vitest-ui-${{ runner.os }}-

      - name: Restore react-devkit build from Turbo cache
        run: |
          # node_modules is restored with --ignore-scripts, so workspace dist/
          # outputs are absent; vitest needs them to resolve sibling packages.
          npx turbo run build --filter=@tetherto/mdk-react-devkit

      - name: Test smaller packages (shard 1 only)
        if: matrix.shard == 1
        # Only react-devkit is sharded; the rest run here once. Packages without
        # a test:coverage task (fonts, catalog app) are skipped by turbo.
        run: |
          npx turbo run test:coverage --filter='!@tetherto/mdk-react-devkit'

      - name: Test react-devkit shard ${{ matrix.shard }}/4
        working-directory: ${{ env.UI_DIR }}/packages/react-devkit
        # Thresholds are disabled per-shard (each shard covers only a subset of
        # files); they are enforced on the merged report in the coverage-ui job.
        run: |
          npx vitest run \
            --shard=${{ matrix.shard }}/4 \
            --coverage \
            --reporter=blob \
            --coverage.thresholds.lines=0 \
            --coverage.thresholds.functions=0 \
            --coverage.thresholds.branches=0 \
            --coverage.thresholds.statements=0

      - name: Upload blob report
        uses: actions/upload-artifact@v4
        with:
          name: ui-blob-report-${{ matrix.shard }}
          path: ${{ env.UI_DIR }}/packages/react-devkit/.vitest-reports/
          retention-days: 1
          if-no-files-found: error
          include-hidden-files: true

  coverage-ui:
    name: 📊 Coverage Merge UI
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [detect-target, setup-ui, test-ui]
    if: needs.detect-target.outputs.run_ui == 'true'
    defaults:
      run:
        working-directory: ${{ env.UI_DIR }}/packages/react-devkit
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-ui.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}

      - name: Download all blob reports
        uses: actions/download-artifact@v4
        with:
          path: ${{ env.UI_DIR }}/packages/react-devkit/.vitest-reports
          pattern: ui-blob-report-*
          merge-multiple: true

      - name: Merge coverage and enforce thresholds
        run: |
          ls -la .vitest-reports/
          npx vitest run --mergeReports --coverage --coverage.reporter=text-summary --reporter=default

  build-ui:
    name: 🏗️ Build UI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [detect-target, setup-ui]
    if: needs.detect-target.outputs.run_ui == 'true'
    defaults:
      run:
        working-directory: ${{ env.UI_DIR }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-ui.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}
      - run: npm run build --if-present

  setup-core:
    name: 📦 Setup Core (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-core]
    if: needs.detect-target.outputs.run_core == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-core.outputs.dirs) }}
    outputs:
      cache-key: ${{ steps.setup-deps.outputs.cache-key }}
      lockfile-outcome: ${{ steps.lockfile.outcome }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Install dependencies and cache node_modules
        id: setup-deps
        uses: ./.github/actions/node-setup-cache
        with:
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}

      - name: Lockfile consistency check
        id: lockfile
        shell: bash
        run: |
          set -euo pipefail
          # For a workspace member the authoritative lockfile is the repo-root
          # one; for a standalone package it is the package's own. Resolve which.
          read -r LOCK_DIR _slug _nm < <(bash "${GITHUB_WORKSPACE}/.github/scripts/workspace-context.sh" "${{ matrix.dir }}")
          if [ ! -f "${LOCK_DIR}/package-lock.json" ]; then
            echo "No package-lock.json in ${LOCK_DIR}; skipping lockfile check"
            exit 0
          fi
          ( cd "${LOCK_DIR}" && npm install --package-lock-only --ignore-scripts --no-audit --no-fund )
          git -C "${GITHUB_WORKSPACE}" diff --exit-code "${LOCK_DIR}/package-lock.json" || (
            echo "::error::package-lock.json (${LOCK_DIR}) is out of sync with package.json."
            echo "::error::Regenerate with: (cd ${LOCK_DIR} && npm install --package-lock-only --ignore-scripts --no-audit --no-fund)"
            exit 1
          )



  lint-core:
    name: ✨ Lint Core (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [detect-target, list-core, setup-core]
    if: needs.detect-target.outputs.run_core == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-core.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-core.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - run: npm run lint --if-present

  typecheck-core:
    name: 🔎 Typecheck Core (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-core, setup-core]
    if: needs.detect-target.outputs.run_core == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-core.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-core.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - run: npm run typecheck --if-present

  test-core:
    name: 🧪 Test Core (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [detect-target, list-core, setup-core]
    if: needs.detect-target.outputs.run_core == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-core.outputs.test_dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-core.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - name: Ensure workspace dependencies (sibling resolution)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          # Core packages relative-require sibling core packages (e.g. mdk ->
          # kernel) and backend/workers/** packages. All are npm workspace
          # members, so a single install at the workspace root hoists their deps
          # and makes the requires resolve. Member jobs already restored the root
          # node_modules above (no-op here); standalone jobs install it here.
          if [ ! -d node_modules ] || [ -z "$(ls -A node_modules 2>/dev/null)" ]; then
            npm ci --ignore-scripts --no-audit --no-fund
            for pkg in sqlite3 bcrypt; do
              if npm ls "$pkg" --depth=99 --parseable 2>/dev/null | grep -qE "(^|/)${pkg}$"; then
                npm rebuild "$pkg"
              fi
            done
          fi
      - name: Test with coverage (enforce ≥80%)
        uses: ./.github/actions/test-with-coverage
        with:
          working-directory: ${{ matrix.dir }}
          min: "80"

  # mdk is sharded across parallel runners: its integration suite (real worker
  # discovery, ~16-40s per test) is too slow for one 20-min job. Shards split by
  # cost — fast unit vs the slow actions-flow suites. Each shard writes its own
  # coverage; coverage-mdk merges them and enforces the ≥80% gate.
  test-mdk:
    name: 🧩 Test Core shard — mdk [${{ matrix.shard }}]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [detect-target, list-core, setup-core]
    if: needs.detect-target.outputs.run_core == 'true'
    strategy:
      fail-fast: false
      matrix:
        # Shard by test group so the slow integration suites run on separate
        # runners (dir-based globs, robust to files being added/removed).
        shard: [unit, miners, containers, other]
    defaults:
      run:
        working-directory: backend/core/mdk
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-core.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: backend/core/mdk
      - name: Ensure workspace dependencies (sibling resolution)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          # mdk relative-requires kernel/client/mdk-worker and backend/workers/**;
          # all are workspace members, hoisted by a single root install. Member
          # jobs already restored the root node_modules above (no-op here).
          if [ ! -d node_modules ] || [ -z "$(ls -A node_modules 2>/dev/null)" ]; then
            npm ci --ignore-scripts --no-audit --no-fund
            for pkg in sqlite3 bcrypt; do
              if npm ls "$pkg" --depth=99 --parseable 2>/dev/null | grep -qE "(^|/)${pkg}$"; then
                npm rebuild "$pkg"
              fi
            done
          fi
      - name: Run shard with coverage
        shell: bash
        env:
          SHARD: ${{ matrix.shard }}
        run: |
          set -euo pipefail
          case "$SHARD" in
            unit)       GLOB='tests/unit/**/*.test.js' ;;
            miners)     GLOB='tests/integration/actions-flow/miners/*.test.js' ;;
            containers) GLOB='tests/integration/actions-flow/containers/*.test.js' ;;
            other)      GLOB='tests/integration/*.test.js' ;;  # top-level only (* is single-level)
            *) echo "::error::unknown shard $SHARD"; exit 1 ;;
          esac
          rm -rf coverage
          NODE_ENV=test npx brittle --coverage "$GLOB"
          [ -f coverage/coverage-final.json ] || { echo "::error::no coverage-final.json for shard $SHARD"; exit 1; }
          cp coverage/coverage-final.json "coverage/mdk-cov-$SHARD.json"
      - name: Upload shard coverage
        uses: actions/upload-artifact@v4
        with:
          name: mdk-cov-${{ matrix.shard }}
          path: backend/core/mdk/coverage/mdk-cov-${{ matrix.shard }}.json
          retention-days: 1

  coverage-mdk:
    name: 📊 Coverage Merge (backend/core/mdk)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [detect-target, test-mdk]
    if: needs.detect-target.outputs.run_core == 'true'
    defaults:
      run:
        working-directory: backend/core/mdk
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Download shard coverage
        uses: actions/download-artifact@v4
        with:
          path: backend/core/mdk/coverage-parts
          pattern: mdk-cov-*
          merge-multiple: true
      - name: Merge coverage and enforce ≥80%
        shell: bash
        run: |
          set -euo pipefail
          MIN=80
          rm -rf .nyc_output && mkdir -p .nyc_output
          npx --yes nyc@17.1.0 merge coverage-parts .nyc_output/out.json
          set +e
          npx --yes nyc@17.1.0 check-coverage --lines=$MIN --statements=$MIN --functions=$MIN --branches=$MIN
          EXIT=$?
          set -e
          REPORT="$(npx --yes nyc@17.1.0 report --reporter=text-summary 2>&1)"
          echo "$REPORT"
          STMT=$(echo "$REPORT" | sed -n 's/^Statements[^:]*: *\([0-9.]*\)%.*/\1/p'); [ -z "$STMT" ] && STMT="—"
          BRANCH=$(echo "$REPORT" | sed -n 's/^Branches[^:]*: *\([0-9.]*\)%.*/\1/p'); [ -z "$BRANCH" ] && BRANCH="—"
          FN=$(echo "$REPORT" | sed -n 's/^Functions[^:]*: *\([0-9.]*\)%.*/\1/p'); [ -z "$FN" ] && FN="—"
          LINES=$(echo "$REPORT" | sed -n 's/^Lines[^:]*: *\([0-9.]*\)%.*/\1/p'); [ -z "$LINES" ] && LINES="—"
          [ $EXIT -eq 0 ] && STATUS="PASS" || STATUS="FAIL"
          # Row consumed by the coverage-summary job (mdk lands in the Core block).
          printf '%s\t%s\t%s\t%s\t%s\t%s\n' "backend/core/mdk" "$STATUS" "$STMT" "$BRANCH" "$FN" "$LINES" > coverage-row.tsv
          exit $EXIT
      - name: Upload coverage row
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: covrow-backend-core-mdk
          path: backend/core/mdk/coverage-row.tsv
          retention-days: 1
          if-no-files-found: ignore

  build-core:
    name: 🏗️ Build Core (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [detect-target, list-core, setup-core]
    if: needs.detect-target.outputs.run_core == 'true' && needs.list-core.outputs.buildable_dirs != '[]'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-core.outputs.buildable_dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-core.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - run: npm run build

  setup-workers:
    name: 📦 Setup Worker (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-workers]
    if: needs.detect-target.outputs.run_workers == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-workers.outputs.dirs) }}
    outputs:
      cache-key: ${{ steps.setup-deps.outputs.cache-key }}
      lockfile-outcome: ${{ steps.lockfile.outcome }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      # node-setup-cache runs first so the lockfile check below uses the pinned
      # NODE_VERSION npm (setup-node), not the runner default — otherwise a
      # different npm reformats the lock and reports a false out-of-sync.
      - name: Install dependencies and cache node_modules
        id: setup-deps
        uses: ./.github/actions/node-setup-cache
        with:
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}

      - name: Lockfile consistency check
        id: lockfile
        shell: bash
        run: |
          set -euo pipefail
          # For a workspace member the authoritative lockfile is the repo-root
          # one; for a standalone package it is the package's own. Resolve which.
          read -r LOCK_DIR _slug _nm < <(bash "${GITHUB_WORKSPACE}/.github/scripts/workspace-context.sh" "${{ matrix.dir }}")
          if [ ! -f "${LOCK_DIR}/package-lock.json" ]; then
            echo "No package-lock.json in ${LOCK_DIR}; skipping lockfile check"
            exit 0
          fi
          ( cd "${LOCK_DIR}" && npm install --package-lock-only --ignore-scripts --no-audit --no-fund )
          git -C "${GITHUB_WORKSPACE}" diff --exit-code "${LOCK_DIR}/package-lock.json" || (
            echo "::error::package-lock.json (${LOCK_DIR}) is out of sync with package.json."
            echo "::error::Regenerate with: (cd ${LOCK_DIR} && npm install --package-lock-only --ignore-scripts --no-audit --no-fund)"
            exit 1
          )

  lint-workers:
    name: ✨ Lint Worker (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [detect-target, list-workers, setup-workers]
    if: needs.detect-target.outputs.run_workers == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-workers.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-workers.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - run: npm run lint --if-present

  typecheck-workers:
    name: 🔎 Typecheck Worker (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-workers, setup-workers]
    if: needs.detect-target.outputs.run_workers == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-workers.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-workers.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - run: npm run typecheck --if-present

  test-workers:
    name: 🧪 Test Worker (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [detect-target, list-workers, setup-workers]
    if: needs.detect-target.outputs.run_workers == 'true'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-workers.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-workers.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - name: Ensure workspace dependencies (sibling resolution)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          # Worker packages relative-require shared core packages (mdk -> kernel,
          # mdk-worker, client) and the mock toolkit. All are workspace members,
          # hoisted by a single root install. Member jobs already restored the
          # root node_modules above (no-op here).
          if [ ! -d node_modules ] || [ -z "$(ls -A node_modules 2>/dev/null)" ]; then
            npm ci --ignore-scripts --no-audit --no-fund
            for pkg in sqlite3 bcrypt; do
              if npm ls "$pkg" --depth=99 --parseable 2>/dev/null | grep -qE "(^|/)${pkg}$"; then
                npm rebuild "$pkg"
              fi
            done
          fi
      - name: Test with coverage (enforce ≥80%)
        uses: ./.github/actions/test-with-coverage
        with:
          working-directory: ${{ matrix.dir }}
          min: "80"

  build-workers:
    name: 🏗️ Build Worker (${{ matrix.dir }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [detect-target, list-workers, setup-workers]
    if: needs.detect-target.outputs.run_workers == 'true' && needs.list-workers.outputs.buildable_dirs != '[]'
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-workers.outputs.buildable_dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-workers.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}
      - run: npm run build

  security_checks:
    name: 🔐 Security Checks
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - name: Dependency Review
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: moderate
          vulnerability-check: true
          license-check: true

  security_audit_ui:
    name: 🔐 Security Audit UI
    if: github.event_name == 'pull_request' && needs.detect-target.outputs.run_ui == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, setup-ui]
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ${{ env.UI_DIR }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-ui.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ env.UI_DIR }}

      - name: Security audit (npm)
        run: |
          set +e
          npm audit signatures 2>&1
          SIGNATURES_EXIT=$?
          npm audit --audit-level=moderate 2>&1
          MODERATE_EXIT=$?
          npm exec --yes -- audit-ci@7 --config "${GITHUB_WORKSPACE}/.github/scripts/audit-ci.jsonc" 2>&1
          HIGH_EXIT=$?
          set -e
          [ $SIGNATURES_EXIT -eq 0 ] && SIGNATURES_STATUS="PASS" || SIGNATURES_STATUS="WARN"
          [ $MODERATE_EXIT -eq 0 ] && MODERATE_STATUS="PASS" || MODERATE_STATUS="WARN"
          [ $HIGH_EXIT -eq 0 ] && HIGH_STATUS="PASS" || HIGH_STATUS="WARN"
          # Only emit a summary block when the audit found something; a clean
          # PR should not produce a wall of PASS tables.
          if [ $MODERATE_EXIT -ne 0 ] || [ $HIGH_EXIT -ne 0 ]; then
            AUD_JSON="${RUNNER_TEMP:-/tmp}/npm-audit-ui.json"
            npm audit --json > "${AUD_JSON}" 2>/dev/null || true
            {
              echo "## Security Audit — UI (\`${{ env.UI_DIR }}\`)"
              echo ""
              echo "| Signatures | Moderate | High/Critical (audit-ci) |"
              echo "|---|---|---|"
              echo "| ${SIGNATURES_STATUS} | ${MODERATE_STATUS} | ${HIGH_STATUS} |"
            } >> "$GITHUB_STEP_SUMMARY"
            bash "${GITHUB_WORKSPACE}/.github/scripts/github-summary-npm-audit-high-critical.sh" "${AUD_JSON}" >> "$GITHUB_STEP_SUMMARY" || true
          fi
          [ $HIGH_EXIT -eq 0 ]

  security_audit_core:
    name: 🔐 Security Audit Core (${{ matrix.dir }})
    if: github.event_name == 'pull_request' && needs.detect-target.outputs.run_core == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-core, setup-core]
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-core.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-core.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}

      - name: Security audit (npm)
        run: |
          set +e
          # Workspace members share the root lockfile/node_modules (see
          # workspace-context.sh); audit-ci needs a lockfile in its cwd to
          # detect the package manager, so audit from the resolved install
          # dir instead of always matrix.dir.
          read -r INSTALL_DIR _slug _nm < <(bash "${GITHUB_WORKSPACE}/.github/scripts/workspace-context.sh" "${{ matrix.dir }}")
          cd "${GITHUB_WORKSPACE}/${INSTALL_DIR}"
          npm audit signatures 2>&1
          SIGNATURES_EXIT=$?
          npm audit --audit-level=moderate 2>&1
          MODERATE_EXIT=$?
          npm exec --yes -- audit-ci@7 --config "${GITHUB_WORKSPACE}/.github/scripts/audit-ci.jsonc" 2>&1
          HIGH_EXIT=$?
          set -e
          [ $SIGNATURES_EXIT -eq 0 ] && SIGNATURES_STATUS="PASS" || SIGNATURES_STATUS="WARN"
          [ $MODERATE_EXIT -eq 0 ] && MODERATE_STATUS="PASS" || MODERATE_STATUS="WARN"
          [ $HIGH_EXIT -eq 0 ] && HIGH_STATUS="PASS" || HIGH_STATUS="WARN"
          # Only emit a per-package summary block when the audit found
          # something; a clean PR should not produce a wall of PASS tables.
          if [ $MODERATE_EXIT -ne 0 ] || [ $HIGH_EXIT -ne 0 ]; then
            SAFE_NAME=$(echo "${{ matrix.dir }}" | tr '/' '-')
            AUD_JSON="${RUNNER_TEMP:-/tmp}/npm-audit-${SAFE_NAME}.json"
            npm audit --json > "${AUD_JSON}" 2>/dev/null || true
            {
              echo "## Security Audit — Core (\`${{ matrix.dir }}\`)"
              echo ""
              echo "| Signatures | Moderate | High/Critical (audit-ci) |"
              echo "|---|---|---|"
              echo "| ${SIGNATURES_STATUS} | ${MODERATE_STATUS} | ${HIGH_STATUS} |"
            } >> "$GITHUB_STEP_SUMMARY"
            bash "${GITHUB_WORKSPACE}/.github/scripts/github-summary-npm-audit-high-critical.sh" "${AUD_JSON}" >> "$GITHUB_STEP_SUMMARY" || true
          fi
          [ $HIGH_EXIT -eq 0 ]

  security_audit_workers:
    name: 🔐 Security Audit Worker (${{ matrix.dir }})
    if: github.event_name == 'pull_request' && needs.detect-target.outputs.run_workers == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [detect-target, list-workers, setup-workers]
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJson(needs.list-workers.outputs.dirs) }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - uses: ./.github/actions/node-restore-cache
        with:
          cache-key: ${{ needs.setup-workers.outputs.cache-key }}
          node-version: ${{ env.NODE_VERSION }}
          working-directory: ${{ matrix.dir }}

      - name: Security audit (npm)
        run: |
          set +e
          # Workspace members share the root lockfile/node_modules (see
          # workspace-context.sh); audit-ci needs a lockfile in its cwd to
          # detect the package manager, so audit from the resolved install
          # dir instead of always matrix.dir.
          read -r INSTALL_DIR _slug _nm < <(bash "${GITHUB_WORKSPACE}/.github/scripts/workspace-context.sh" "${{ matrix.dir }}")
          cd "${GITHUB_WORKSPACE}/${INSTALL_DIR}"
          npm audit signatures 2>&1
          SIGNATURES_EXIT=$?
          npm audit --audit-level=moderate 2>&1
          MODERATE_EXIT=$?
          npm exec --yes -- audit-ci@7 --config "${GITHUB_WORKSPACE}/.github/scripts/audit-ci.jsonc" 2>&1
          HIGH_EXIT=$?
          set -e
          [ $SIGNATURES_EXIT -eq 0 ] && SIGNATURES_STATUS="PASS" || SIGNATURES_STATUS="WARN"
          [ $MODERATE_EXIT -eq 0 ] && MODERATE_STATUS="PASS" || MODERATE_STATUS="WARN"
          [ $HIGH_EXIT -eq 0 ] && HIGH_STATUS="PASS" || HIGH_STATUS="WARN"
          # Only emit a per-package summary block when the audit found
          # something; a clean PR should not produce a wall of PASS tables.
          if [ $MODERATE_EXIT -ne 0 ] || [ $HIGH_EXIT -ne 0 ]; then
            SAFE_NAME=$(echo "${{ matrix.dir }}" | tr '/' '-')
            AUD_JSON="${RUNNER_TEMP:-/tmp}/npm-audit-${SAFE_NAME}.json"
            npm audit --json > "${AUD_JSON}" 2>/dev/null || true
            {
              echo "## Security Audit — Workers (\`${{ matrix.dir }}\`)"
              echo ""
              echo "| Signatures | Moderate | High/Critical (audit-ci) |"
              echo "|---|---|---|"
              echo "| ${SIGNATURES_STATUS} | ${MODERATE_STATUS} | ${HIGH_STATUS} |"
            } >> "$GITHUB_STEP_SUMMARY"
            bash "${GITHUB_WORKSPACE}/.github/scripts/github-summary-npm-audit-high-critical.sh" "${AUD_JSON}" >> "$GITHUB_STEP_SUMMARY" || true
          fi
          [ $HIGH_EXIT -eq 0 ]

  coverage-summary:
    name: 📊 Coverage Summary (per domain)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always() && !cancelled()
    needs: [detect-target, test-core, test-mdk, coverage-mdk, test-workers]
    steps:
      - name: Download coverage rows
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          path: rows
          pattern: covrow-*
      - name: Render per-domain coverage tables
        shell: bash
        run: |
          set -euo pipefail
          find rows -name coverage-row.tsv -exec cat {} + > all.tsv 2>/dev/null || true
          if [ ! -s all.tsv ]; then
            echo "No coverage rows were produced this run." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          pct () { [ "$1" = "—" ] && printf '—' || printf '%s%%' "$1"; }
          render () {
            local title="$1" prefix="$2" body
            body=$(grep -E "^${prefix}" all.tsv || true)
            [ -z "$body" ] && return 0
            {
              echo "## Coverage — ${title}"
              echo ""
              echo "| Package | Status | Statements | Branches | Functions | Lines |"
              echo "|---|:--|--:|--:|--:|--:|"
              while IFS=$'\t' read -r pkg status stmt br fn ln; do
                [ -z "$pkg" ] && continue
                case "$status" in
                  PASS) icon="✅" ;; FAIL|TESTS-FAILED) icon="❌" ;; *) icon="⚠️" ;;
                esac
                echo "| \`${pkg}\` | ${icon} ${status} | $(pct "$stmt") | $(pct "$br") | $(pct "$fn") | $(pct "$ln") |"
              done <<< "$body"
              echo ""
            } >> "$GITHUB_STEP_SUMMARY"
          }
          render "Core" "backend/core/"
          render "Workers" "backend/workers/"

  summary:
    name: 📋 CI Summary
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always() && !cancelled()
    needs:
      - detect-target
      - security_checks
      - security_audit_ui
      - security_audit_core
      - security_audit_workers
      - setup-ui
      - lint-ui
      - typecheck-ui
      - test-ui
      - coverage-ui
      - build-ui
      - list-core
      - setup-core
      - lint-core
      - typecheck-core
      - test-core
      - test-mdk
      - coverage-mdk
      - build-core
      - list-workers
      - setup-workers
      - lint-workers
      - typecheck-workers
      - test-workers
      - build-workers
      - list-examples
      - setup-examples
      - test-examples
      - coverage-summary
    steps:
      - name: Write job summary
        shell: bash
        env:
          REASON: ${{ needs.detect-target.outputs.reason }}
          RUN_UI: ${{ needs.detect-target.outputs.run_ui }}
          RUN_CORE: ${{ needs.detect-target.outputs.run_core }}
          RUN_WORKERS: ${{ needs.detect-target.outputs.run_workers }}
          RUN_EXAMPLES: ${{ needs.detect-target.outputs.run_examples }}
          SEC: ${{ needs.security_checks.result || 'skipped' }}
          SEC_UI: ${{ needs.security_audit_ui.result || 'skipped' }}
          SEC_CORE: ${{ needs.security_audit_core.result || 'skipped' }}
          SEC_WRK: ${{ needs.security_audit_workers.result || 'skipped' }}
          UI_SETUP: ${{ needs.setup-ui.result || 'skipped' }}
          CORE_SETUP: ${{ needs.setup-core.result || 'skipped' }}
          WRK_SETUP: ${{ needs.setup-workers.result || 'skipped' }}
          EX_SETUP: ${{ needs.setup-examples.result || 'skipped' }}
          EX_TEST: ${{ needs.test-examples.result || 'skipped' }}
        run: |
          ic() {
            case "$1" in
              success) echo "✅" ;;
              failure) echo "❌" ;;
              cancelled) echo "🚫" ;;
              skipped) echo "⏭️" ;;
              *) echo "❔" ;;
            esac
          }
          {
            echo "## CI Summary (mdk monorepo)"
            echo ""
            echo "**Selection:** ${REASON} (ui=${RUN_UI} core=${RUN_CORE} workers=${RUN_WORKERS} examples=${RUN_EXAMPLES})"
            echo ""
            echo "| Area | Setup | Security |"
            echo "|---|---|---|"
            echo "| 🔐 Dependency review | — | $(ic "${SEC}") \`${SEC}\` |"
            echo "| 🎨 UI | $(ic "${UI_SETUP}") \`${UI_SETUP}\` | $(ic "${SEC_UI}") \`${SEC_UI}\` |"
            echo "| 🧱 Core | $(ic "${CORE_SETUP}") \`${CORE_SETUP}\` | $(ic "${SEC_CORE}") \`${SEC_CORE}\` |"
            echo "| ⚙️ Workers | $(ic "${WRK_SETUP}") \`${WRK_SETUP}\` | $(ic "${SEC_WRK}") \`${SEC_WRK}\` |"
            echo "| 📋 Examples | $(ic "${EX_SETUP}") \`${EX_SETUP}\` (test: $(ic "${EX_TEST}") \`${EX_TEST}\`) | — |"
            echo ""
            echo "### What ran"
            echo "- **Lockfile consistency** in setup jobs (root lock for workspace members, own lock for standalone packages)"
            echo "- **Workspace-aware install**: members install once at the repo root and share the hoisted \`node_modules\` cache; standalone packages install in place"
            echo "- **PR:** dependency review + per-package npm audits (audit-ci for high/critical)"
          } >> "$GITHUB_STEP_SUMMARY"
